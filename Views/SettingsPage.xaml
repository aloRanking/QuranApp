<rxui:ReactiveContentPage
    x:Class="QuranApp.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:rxui="clr-namespace:ReactiveUI.Maui;assembly=ReactiveUI.Maui"
    xmlns:viewmodel="clr-namespace:QuranApp.ViewModels"
    Title="Settings"
    x:TypeArguments="viewmodel:SettingsViewModel">

    <Grid BackgroundColor="{Binding Theme.CurrentTheme.Background}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
<StackLayout Padding="20">
    <StackLayout.Resources>
                <Style TargetType="Label">
                    <Setter Property="TextColor" Value="{ DynamicResource PageTextColor}" />
                </Style>
                
            </StackLayout.Resources>

    <Slider Minimum="0"
            Maximum="11"
            Value="{Binding Theme.ThemeLevel}" />

    
    <Grid ColumnDefinitions="*,*,*">
        <Label Text="Light" HorizontalOptions="Start" />
        <Label Text="Middle" HorizontalOptions="Center" Grid.Column="1"/>
        <Label Text="Dark" HorizontalOptions="End" Grid.Column="2"/>
    </Grid>
</StackLayout>
        

        <Label Grid.Row="1"
               Text="Testing!"
               TextColor="{Binding Theme.CurrentTheme.Text}"
               HorizontalOptions="Center"
               VerticalOptions="Center" />
    </Grid>
</rxui:ReactiveContentPage>